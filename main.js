let cnv = document.getElementById("myCanvas");
let ctx = cnv.getContext("2d");
cnv.width = 1000;
cnv.heigth = 500;

let bubble = {x: 400, y: 300, r: 25}

//requestAnimationFrame(draw);
fill("blue");
circle(10,10,25,"fill");
///fill("blue");
//circle(bubble.x, bubble.y , bubble.r, "fill");
    //requestAnimationFrame(draw);
